<%# ----- SQUAD HEADER ----- %>
<div class="banner overflow-hidden rounded-4 shadow-sm" style="position: relative;"id="subcard">
<%= image_tag "ligne.jpg", class: "img-fluid", style: "object-fit: cover" %>
<div class="text-center text-white" style="position: absolute; top: 50%; left: 50%; transform: translate(-50%, -50%);">
<h1>Plutôt Winner ou Looser ?</h1>
</div>
</div>

<div class="row d-flex justify-content-center mb-4">
  </div>

  <div class="row d-flex justify-content-center mb-4">
  <div class="grid-1">
      <div class="grid-3">
        <div class=" rounded-4 p-3 shadow" id="subcard">
            <h2 class="text-white text-center"><%= current_user.points_counter %></h2>
            <p class="text-white text-center">points</p>
        </div>

        <div class=" rounded-4  p-3 shadow" id="subcard">
            <h2 class="text-white text-center"><%= current_user.bets.where(progress: 'won').count %></h2>
            <p class="text-white text-center">paris gagnés</p>
        </div>

        <div class=" rounded-4  p-3 shadow" id="subcard">
            <h2 class="text-white text-center"><%= current_user.bets.count %></h2>
            <p class="text-white text-center">paris joué</p>
          </div>
        </div>
      </div>
  </div>
  <div class="row">
    <div class="grid-2-1 mb-3">
      <div class="d-flex flex-column rounded-4 shadow p-3" id="subcard">
        <div class="text-center col-12"  >
          <h2 class=" text-white">Classement</h2>

          <% current_user.squad.users.sort_by { |user| user.points_counter }.reverse.each do |user| %>

            <div class="classement-list d-flex  justify-content-evenly align-items-center mb-2 text-white">
              <p>🏅</p>
              <p><%= user.nickname %></p>
              <p><strong><%= user.bets.where(progress: 'won').count %>/<%= user.bets.count %></strong> paris gagnés</p>
              <p><%= image_tag "picto/icon-points.svg", class:"me-2 align-items-center" %><strong><%= user.points_counter %> </strong>points</p></p>
            </div>
          <% end %>

        </div>
      </div>

      <div class="d-flex justify-content-center shadow rounded-4 p-3", id="subcard">
        <% if current_user.bets.where(progress: 'won').count >= current_user.bets.where(progress: 'lost').count %>
          <h2 class=" text-white">Le roi de la lucarne</h2>
        <% else %>
          <h2 class=" text-white">Le maestro des poteaux</h2>
        <% end %>
      </div>

    </div>
  </div>
